# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
.next/
out/
next-env.d.ts
*.tsbuildinfo
/coverage
.vercel

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Playwright MCP session artifacts (screenshots/snapshots from verification runs)
.playwright-mcp/

# Playwright test-run artifacts (traces, screenshots on failure, HTML report)
/test-results/
/playwright-report/

# Curator LLM-response cache (src/server/services/curator.ts, Phase 3.3) — cache-aside, one file
# per item keyed on model+prompt-version+source id; reproducible by re-running curateItems().
.cache/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Phase 0 harvester response cache + generated outputs (reproducible via embed.ts / build-explore.ts / curate.ts / build-feed.ts)
phase0/.cache/
phase0/.cache-curate/
phase0/vectors/
phase0/explore.html
phase0/feed.html
phase0/items.curated.json

# Python (Phase 0 prototype scripts)
__pycache__/
*.pyc
.venv/
venv/

# Design source archives (extracted copies are committed under docs/)
Ambit Redesign.zip
