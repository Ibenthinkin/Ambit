# Beta feedback — Phase 8.2 T6

_One row per impression, in the friend's own words. Appended through the watched week (T6.4)
from texts, calls and over-the-shoulder moments. `screen` uses the app's route names: `/feed`,
`/i/[id]`, `/saved`, `/profile`, `/profile/topics`, `/profile/settings`, `/onboarding`, landing.
`triage` is filled at the end of the week (T6.5): `9.x` (an existing or new BUILD_PLAN Phase 9
item), `fix now` (a short list at the top of the 8.2 walkthrough), or `no` (with a one-clause
reason). Names never go in this file — a role is enough (`friend, designer`, `sibling, non-tech`)._

| date | who (role) | screen | what they said | triage |
|---|---|---|---|---|
