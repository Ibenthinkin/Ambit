# Ambit — Candidate Source APIs

> A curated **backlog** of public content APIs to trial as feed sources _after_ the MVP ships. These are **not** committed v1 sources — the committed set lives in `SPEC.md` §6.1. Add candidates here freely; promote one into the SPEC only after it passes the trial loop below.

## What makes a good Ambit source

Ambit's feed is public-domain / openly-licensed **images and short text** that normalize into the common `Item` schema (see SPEC §5.1). A candidate is worth trialing only if it clears this bar:

- **Item fit** — yields image items or short-text items (title + summary/lede). Not data feeds (weather, finance, sports).
- **License** — public domain (CC0) or open with clear attribution. Aggregators mix licenses — you must be able to _filter to PD/CC0/open at query time_.
- **Access** — no-auth or free API key; humane rate limits; ~$0 cost.
- **Density** — enough varied content across our topic chips to be worth an adapter.
- **Serendipity** — because relatedness is embeddings-led and **cross-source**, the real test is whether jumps _into and out of_ this source feel inspired, not just whether its own content is good.

## Trial loop (per candidate)

Adding a source is intentionally isolated — one `SourceAdapter` module, idempotent ingestion (SPEC §6.1). To trial one:

1. **Write the adapter** — `search(query)` + `toItem(raw)` → common `Item` shape.
2. **Ingest a sample** — run it against a handful of topic seed queries.
3. **Eyeball** — content density, the **curation-score distribution against the existing corpus**, image-fetch health, and the rendered surfaces (`/i/` for a few items, `/feed` under `FEED_DEBUG` for context). _(Updated 08-21-26: this step used to say "whether embedding nearest-neighbors across existing sources feel good", which predates Phase 0.4's pivot — item-level NN was tested and rejected, and nothing at request time touches embeddings. Score distributions plus rendered surfaces are the current-architecture version of the same question.)_ _(Cut 1, 09-02-26: for a **walk** source, also read what its **un-homed** items are about — the ingest summary's `top tags among them` line, or `bun run stats:walk`'s `un-homed tags`. Under the vocabulary-growth principle those are the source's contribution to the topic vocabulary, not waste; a high un-homed share with a coherent tag cluster is evidence **for** a new topic, not against the source.)_
4. **Decide** — **Keep** (promote to SPEC §6.1, full seed cells, full ingest), **Park** (needs work — note why), or **Cut** (leave here struck through with the reason).

Trial one or a few at a time; **never batch-add without eyeballing**. Round 1 (Phase 6.2, 08-21-26) is the worked example: four candidates, a written evidence sheet, then a stop for Ben's four verdicts before anything was promoted — `docs/PHASE6_WALKTHROUGH_6.2.md`.

**Round 2 (09-01-26)** covered every remaining 🔵 untried API candidate plus the untried blog leads with a live pre-trial probe (step 0, not the full loop — no adapter written, nothing ingested): real curl/API calls against current endpoints, checking whether license-filterability, auth, and item-fit actually hold up. Two came back genuinely trial-ready (**Europeana**, **Openverse** — both have a real, verified query-time license filter and a clean single-call item shape); **Chronicling America** extends the already-kept LoC pattern; **thisiscolossal.com** is a near drop-in for the existing WP-REST blog adapter. The rest parked or cut on structural grounds found empirically rather than assumed — see each row below. Nothing here was promoted; that still needs an actual adapter + ingest + score-distribution pass and Ben's verdict, per the loop above.

## Committed v1 sources (for reference — see SPEC §6.1)

Wikipedia · Met Museum · ~~Art Institute of Chicago~~ (suspended) · Cleveland Museum of Art · Wellcome Collection · `archive` (Ben's personal archive, Phase A.5) · **Smithsonian Open Access · Library of Congress · NASA Image Library** (promoted 08-21-26, Phase 6.2).

> **Corrected 08-20-26.** This line previously read "Wikipedia · Met · AIC · Smithsonian Open Access · Project Gutenberg / Wikisource · Wikiquote · NASA APOD · Public Domain Review", which was never the built set — it looks like an early draft that outlived the decision. Four of those eight have no adapter and are *candidates*, not commitments; treating Smithsonian as already-integrated is exactly the mistake this file exists to prevent. The real v1 set is the five in `server/services/sources/index.ts`.
>
> **AIC is suspended** as of 08-20-26 (`server/config/suspended-sources.ts`) — ingestion skips it and the feed won't draw its rows. Not a cut: the adapter and all 1,338 rows stay, and Phase 5.7's image proxy is expected to lift it. See SPEC §6.1.

## Candidates

Legend — **Type:** 🖼️ image · 📝 text · 🔀 both. **Auth:** none / key (free). **Status:** 🔵 untried · 🟡 parked · ✅ kept · ❌ cut.

| Source                                       | Type | License                    | Auth                       | Status | Why interesting / notes                                              |
| -------------------------------------------- | ---- | -------------------------- | -------------------------- | ------ | -------------------------------------------------------------------- |
| Cleveland Museum of Art Open Access          | 🖼️   | CC0                        | none                       | ✅     | **Kept — promoted to SPEC §6.1 (Phase 0.5 trial).** Friendliest API of the set: `limit` ≤1000, `cc0` flag, prose `description` on many objects. 1,638 items harvested, strong curation scores. |
| Rijksmuseum API                              | 🖼️   | Public domain (mostly)     | key                        | 🟡     | **Parked 09-01-26 — wants the same design decision as Getty.** The old tutorial API (demo key `0fiuZFh4`) is dead — HTTP 410 Gone, permanently retired; every "public demo key" tutorial online targets this dead endpoint. What's live now (`data.rijksmuseum.nl/docs/search`) needs **no key at all**, and real CC0/PDM rights (`creativecommons.org/publicdomain/...` URIs) are genuinely present on objects — but only after a 3–4-hop Linked Art/JSON-LD graph traversal per item (search hit → object → `VisualItem` → `DigitalObject` just to reach an image URL and its rights statement), worse than Getty's single SPARQL query. Fits neither blessed integration shape any more cleanly than Getty does — decide the two together rather than adapting around the graph shape piecemeal. |
| Harvard Art Museums API                      | 🖼️   | Mixed — filter to open     | key                        | 🟡     | **Parked 09-01-26 — leaning cut.** Live, but genuinely key-gated: no self-serve signup, only a Google Form with unknown turnaround (unlike Smithsonian/DPLA's instant `api.data.gov`/email-key flow). Worse, there is **no license/rights filter of any kind** in the documented `/object` query params — `copyright` is a free-text field that's often literally `"President and Fellows of Harvard College"` even on old-looking works, i.e. Harvard asserting rights over the photography rather than declaring the underlying work PD. And its Terms of Use **cap cached storage at two weeks without written permission** — directly incompatible with Ambit's permanent-corpus architecture unless Harvard grants an exception. 2,500 calls/day, non-commercial-use-only. Park unless Ben wants to write for a storage exception. |
| Wellcome Collection API                      | 🔀   | Open (CC, filterable)      | none                       | ✅     | **Kept — promoted to SPEC §6.1 (Phase 0.5 trial).** History-of-science texture the art museums lack. Traps recorded in phase0/NOTES.md: thumbnail URL locked to 200px (rewrite the IIIF size segment); license per item (`cc-0,cc-by,pdm` filter + per-work check); thin summaries (many title-page stubs — the curator handles them). |
| Openverse API                                | 🖼️   | CC / PD (filterable)       | none (key = higher limits) | 🔵     | Aggregates openly-licensed images across sources — filter to CC0/PD. **Probed 09-01-26, looks ready for a real trial**: no-auth works today (20/min, 200/day observed), and `license=cc0,pdm` genuinely filters — verified against an unfiltered baseline for the same query, which pulled in `by`/`by-nc`/`by-nc-nd`. Item shape is close to feed-ready with almost no rewriting needed (`thumbnail` is Openverse's own proxy and already resolves; a ready-made `attribution` string ships on every result). One flooding gotcha to design around before ingest: 13 of the top 20 `astronomy`+`cc0` results came from just two prolific uploaders (Flickr + Wikimedia Commons) — the same specimen-flood shape Smithsonian hit, needing per-uploader diversity caps. The license label is Openverse's own crawl-time assertion of the *source's* metadata, not a live re-check — same trust posture already recorded here for CMA/Wellcome/Smithsonian. |
| NASA Image & Video Library (images.nasa.gov) | 🖼️   | Public domain              | none                       | ✅     | **Kept 08-21-26 — promoted to SPEC §6.1.** Trialed in place of APOD, which earlier BUILD_PLAN drafts named: the whole catalogue, no auth, versus one keyed image a day. 520 items @ 7.96 avg across six topics, clean images, no rewrite needed (renditions ship as explicit `links[]` with widths). **The licensing answer is "stated, not filtered"**: a 600-item survey found *no rights field of any kind*, so `license` is the literal `"Public domain (NASA)"` and `attribution` reproduces NASA's own credit line verbatim rather than flattening to the agency name. Exactly 2 of 600 credited a non-NASA party. Kept with that stated plainly rather than parked on it. |
| Library of Congress (loc.gov JSON)           | 🔀   | Mostly PD (check per-item) | none                       | ✅     | **Kept 08-21-26 — promoted to SPEC §6.1**, scoped to cleared collections. The 08-20 gap was confirmed, not closed: the per-result `rights` field is empty on every row. The adapter therefore never searches all of loc.gov — `CLEARED_COLLECTIONS` holds collections the Library has blanket-cleared, each carrying its rights statement **verbatim** ("No known restrictions on publication" for `mrg`, deliberately not upgraded to "public domain"), and every result's own `collection[]` membership is re-checked. **The verdict was about the pattern as much as the collection**: the list grows one verified rights statement at a time. First entry `mrg` (Margolies, 11,708 images) scored 8.52 avg at sample scale with every item ≥8 — the best in the corpus — and 7.98 across 376. One caution carried into SPEC §15: `tile.loc.gov` rate-limits by IP with no `Retry-After`, and a 334-image ingest tripped a sustained 429. |
| Chronicling America (LoC newspapers)         | 🔀   | Public domain              | none                       | 🔵     | Historic newspaper pages + OCR text — great "old world" texture. **Probed 09-01-26**: the doc's old `chroniclingamerica.loc.gov` endpoint is dead (redirects to `www.loc.gov/chroniclingamerica/`); the real path is the unified `www.loc.gov/collections/chronicling-america/?q=<query>&fo=json`, same `fo=json` convention the shipped `loc.ts` adapter already uses. **Not uniformly PD despite this row's own License column** — the per-result `rights` field is absent entirely (worse than the pictures adapter's empty-string gap), so PD has to be enforced by a hard `date` cutoff (currently ~Jan 1931), and it does real work: a `serendipity` sample skewed 1870s–1960s with only 29% falling before the line. OCR (`description[]`, inline in the search hit — no per-page call needed) is real but garbled ("Atsiatant Editor") — plan to synthesize summaries from structured metadata (title/date/paper/place) rather than raw OCR, the same call already made for LoC pictures. Images share `tile.loc.gov`'s known rate limit; requesting `full/full` 503s where `full/pct:50` works — ask for a percentage, not the `full` keyword. **Worth a decision alongside this, unrelated to Chronicling America itself**: `www.loc.gov/robots.txt` disallows `/search` and `/pictures/search` for `*`, and the *already-shipped* `loc.ts` adapter's endpoint lives under `/pictures/search/` — flagging for Ben, not fixed here. |
| Internet Archive (advancedsearch + metadata) | 🔀   | Mixed — filter to PD/open  | none                       | 🟡     | **Parked 09-01-26.** Live, no auth, and the license story is better than "needs careful filtering" implies — `licenseurl:*publicdomain*` is a real Solr query-time filter, verified: narrowed a `botanical`+image search from 2,060 to 305 with every sampled row already PD/CC0-shaped. But three real costs remain before it's a cheap trial: (1) ~30% of items carry **no** rights field at all and fall back to a per-item `/metadata` call anyway; (2) even the "has a Creative Commons URL" set is genuinely mixed (69 by, 43 by-nc-sa, 16 pdm, only 2 true cc0 in one 200-item sample) — a naive "has a CC link" filter would leak non-commercial/no-derivative licenses through; (3) relevance-sorted results are dominated by prolific single uploaders worse than Smithsonian's specimen flood (7 of the top 10 `astronomy` hits were one uploader's templated diagram series). No canonical image field either — picking the right file out of `files[]` (vs. CMA/NASA's explicit `links[]`) is adapter work, and `description` carries raw HTML needing `htmlToText()` from day one. |
| Open Library API                             | 🔀   | Metadata open; covers vary | none                       | 🟡     | **Parked 09-01-26 — same prerequisite as PoetryDB: no text-curation rubric.** Live, no auth. Cover images are real and reliable when `cover_i` is present (confirmed live), but it isn't universal (4/5 in one subject sample). The real gap is the lede: `search.json`'s `first_sentence` field exists on canonical heavily-catalogued works but is **absent on most records** (3 of 5 in the same sample had title+author+cover and zero text) — a real prose `description` only lives on the separate per-work `/works/{id}.json` endpoint, a second per-item call in the Wellcome/CMA hydration pattern, and it's frequently empty there too. Requesting `description` directly from `search.json` 500s rather than returning empty — an undocumented footgun. Un-parking needs both the text-curation rubric SPEC §15 already calls for, and a hydration design like Wellcome's. |
| PoetryDB                                     | 📝   | Public domain              | none                       | 🟡     | **Parked 08-21-26 — two fixable reasons, neither the poems' fault.** Adapter and tests stay in-repo; what it lacks is seed cells, so ingest never reaches it. (1) Summaries take the poem's first two lines and PoetryDB's `lines[]` includes epigraphs and dedications, so a card can lead with transliterated Greek. (2) **The curator has no rubric for text** — Pope and Seeger scored 4 against a prompt asking for "visually striking" images; the 5.50 average says more about the prompt than the corpus (SPEC §15). Un-parking = giving it cells again. API quirk worth keeping either way: `GET /lines/<kw>` **503s at any real result-set size** (nine keywords tested), so search is a two-step — `/lines/<kw>/title,author`, then per-poem hydration — and a no-match is a JSON *object* at HTTP 200. |
| Wikiquote API                                | 📝   | CC BY-SA                   | none                       | 🟡     | **Moved here 08-21-26 from BUILD_PLAN 6.2**, where it sat in a "remaining v1 adapters" list that the 08-20 correction established was never the committed set. Short quotations with attribution — cheap text items for the `poetry`/`mythology` end of the chip grid. CC BY-SA is share-alike, unlike anything in the v1 set. **Probed 09-01-26 — parked, needs two things before an adapter is well-defined.** No clean per-quote JSON exists: `prop=extracts` flattens the entire page (bio + every quote across eras) into one run-on plain-text blob with citations mashed inline; getting individual quotes with attribution means parsing `wikitext`'s bullet-list structure (quote = `*`, citation = `**`) by hand, plus stripping `[[File:...]]`/wikilink noise — real parsing work, not a `search()`+`toItem()` mapping. Anonymous MediaWiki traffic also hit a hard rate limit after roughly a dozen calls even with a descriptive User-Agent — worth checking whether the shipped Wikipedia adapter's identification already clears this. Needs the text-curation rubric (SPEC §15) and a wikitext-quote-extraction design, in either order. |
| Project Gutenberg / Wikisource               | 📝   | Public domain              | none                       | 🟡     | **Moved here 08-21-26 from BUILD_PLAN 6.2**, same reason. Full public-domain texts — the item-fit question is what a *feed-sized* item is when the source unit is a book, which is the same question PoetryDB answered badly and blogs will ask again. Worth trialing only after SPEC §15's "the curator has no rubric for text items" is settled. **Probed 09-01-26.** Gutenberg itself has no first-party JSON API; the common third-party mirror **Gutendex is unreachable from a headless ingest script** — every request (root, `/books`, `/books/?search=`) hit a Cloudflare *managed JS challenge*, 403, confirmed with both a bare and full-browser User-Agent — not a rate limit, a hard wall, so treat Gutendex itself as ❌ cut. **Wikisource confirms the item-fit question rather than answering it**: it's live (same Wikimedia rate-limit wall as Wikiquote above), and *Alice's Adventures in Wonderland* does have real chapter subpages — but every chapter runs 1,746–2,715 words, a 7–12 minute read, not a card. There is no unit between "whole book" and "whole chapter" exposed by the API; an adapter would have to invent an excerpting policy (opening paragraph? one famous passage?) with nothing to score it against. Un-parking needs the text-curation rubric plus a feed-sized-unit decision for book sources — the same two blockers already on record here, now confirmed empirically rather than assumed. |
| Europeana API                                | 🔀   | Mixed — filter to open     | key                        | 🔵     | Aggregates EU cultural heritage; rich but license-heterogeneous. **Probed 09-01-26 — the most trial-ready candidate in this round.** The historical demo key (`wskey=api2demo`) still works live, right now, no signup — and `reusability=open` is a real query-time filter, proven against live data: an unfiltered `sunflowers` search pulled in an *In Copyright — Educational Use* item, and adding the filter to a `flowers` query returned only CC0/PDM rights URIs in the sample checked. Field name is the literal `rights` array, directly on the search result — single-call filtering, no per-item second lookup, the good pattern (unlike CMA/Wellcome's early traps). Item shape is complete in one call: title, `dcDescription`, `edmIsShownBy` (direct image URL), `edmPreview` (Europeana's own thumbnail proxy), `dataProvider`/`provider` for attribution. Demo-key rate limit observed at 1,000,000/hr (shared pool, not necessarily what a personal key gets). Two things still open, not blocking: personal-key signup friction is unverified (`pro.europeana.eu`'s docs are Cloudflare-gated to curl — needs a browser follow-up), and the `reusability=open` sample was small (3 items) — worth the same few-hundred-row sweep Smithsonian's CC0 filter got before trusting it at ingest scale. |
| DPLA API                                     | 🔀   | Mixed — filter to open     | key                        | 🟡     | **Parked 09-01-26 — un-parking is nearly free.** Correction to "key by request": it's actually **instant self-service** — `POST api.dp.la/v2/api_key/<email>` emails a key immediately, no manual review, confirmed live. No documented rate limit either (DPLA's policy explicitly declines to set one, reserving only the right to revoke abusive use). What's actually blocking a verdict: the docs site (`pro.dp.la`/`dp.la/info`) 403s from a CloudFront WAF to both curl and browser-UA fetches, so the current response shape couldn't be confirmed live — DPLA's own published examples show `object`/`isShownAt` as **nested objects in older docs, flat strings in newer ones**, real shape drift an adapter needs to defend against either way. Rights are RightsStatements.org URIs *recommended*, not required, across ~4,000 contributing institutions — expect the same per-item mix Internet Archive and LoC already hit. Un-parking = retrieve the emailed key and run two real sample searches; nothing else is blocking. |
| Biodiversity Heritage Library API            | 🔀   | Public domain              | key                        | 🟡     | **Parked 09-01-26 — wants a Getty-style design decision, not just a key.** The api3 key-request form (`getapikey.aspx`) and docs (`docs/api3.html`) are behind a hard Cloudflare challenge to any agent tooling — turnaround unknown, needs a human at the form. But probing surfaced a real alternative: BHL publishes a fully **no-auth bulk export** (`s3://bhl-open-data`, AWS Open Data since 2024) — gzipped TSVs (`item.txt`, `page.txt`) plus per-page OCR and JP2 images, already pre-filtered to exclude in-copyright content (its own README states this) with a per-item `CopyrightStatus`/`RightsStatement` breakdown. Item-fit is the same document/page-scan corpus as loc.gov/Chronicling America — no clean artwork+caption unit (one sampled `page.txt` batch was 92% Text, only 4% Illustration), OCR is worse than newspapers (Latin binomials + Greek etymologies mangle badly), and images are JPEG2000, needing a pipeline conversion step nothing currently does. Neither the api3 key route nor the S3 bulk route is cleanly search-shaped or corpus-walk-shaped per CLAUDE.md's "don't invent a third shape" — decide the two routes deliberately, the way Getty is already parked pending one. |
| **Smithsonian Open Access** (api.si.edu)     | 🔀   | CC0 (filterable)           | key (free, api.data.gov)   | ✅     | **Kept 08-21-26 — promoted to SPEC §6.1** (first run of the trial loop; evidence in `docs/PHASE6_WALKTHROUGH_6.2.md`). The 08-20 probe held up: `media_usage:"CC0"` is a query filter and 400/400 sampled rows carried `usage.access: "CC0"` — no per-item second call, the trap CMA and Wellcome each cost us. What the trial *added*: **2 of those 400 also carried `online_media_rights: ["Copyright protected/restricted"]`**, so both signals are re-checked; and the density question was answered — the specimen flood is real, 31% of offered items die at the structural floor, and seed vocabulary (never `specimen`, 5M rows) is what carries the cell. 1,529 items @ 7.73 avg, 14 of 16 topics. Real key raises the limit to 1,000/hr. |
| **Getty Museum** (data.getty.edu)            | 🖼️   | Open Content = PD, no restrictions | none                | 🟡     | **Parked — wants a design decision before an adapter.** Real and unauthenticated (verified 08-20-26: the SPARQL endpoint answers a CIDOC-CRM query with JSON results, and `/museum/collection/docs/` is live). But **it is a Linked Art / JSON-LD knowledge graph, not a search API** — objects are JSON-LD documents at UUID URLs, and free-text discovery goes through SPARQL. That fits *neither* blessed integration shape (search-shaped or corpus-walk, per CLAUDE.md's ecosystem section), so trialing it means either writing SPARQL that emulates `search(q)` or treating it as a corpus walk — a call to make deliberately, not mid-adapter. Content quality is not in doubt; the shape is. |
| Artvee                                       | 🖼️   | PD (curated)               | none — **no API**          | ❌     | **Cut 08-20-26.** Two independent reasons. There is no public API — it is a browsable site, so ingestion would mean scraping. And its `robots.txt` deploys the "Ultimate AI Block List v1.7", explicitly disallowing AI/agent user-agents. A source whose operator has stated that preference in machine-readable form is not one Ambit should be taking, and a hand-rolled scraper around it is worse. The underlying works are public domain and reachable through sources that *do* offer APIs — that is the route in, if the imagery is what appeals. |
| Cooper Hewitt, Smithsonian Design Museum     | 🖼️   | CC0 (per object)           | key (broken)                | ❌     | **Cut 09-01-26 — redundant, not just broken.** The standalone API (`api.collection.cooperhewitt.org`) is live-but-non-functional: every REST call, any method, 500s. Its docs (`collection.cooperhewitt.org/api/`) 301 into `www.si.edu`, which bot-walls with a 403 "request verification" page. But the decisive finding is redundancy, not access: querying the **already-kept** Smithsonian Open Access API for `unit_code:CHNDM` returns **58,198 Cooper Hewitt objects right now** — complete, feed-ready, filterable with the exact same `online_media.media[].usage.access: "CC0"` check the shipped Smithsonian adapter already runs. Design-museum texture (furniture, textiles, wallpaper, product design) genuinely distinct from the rest of the corpus — but the route in is widening the existing Smithsonian adapter's `unit_code`/seed-query handling, not a new adapter. |

## Leads, not sources (triaged 08-20-26)

Ben's 08-20 batch. Four of the thirteen URLs were APIs and moved into the table above (Smithsonian, Getty, Artvee, and the Margolies collection folded into the LoC row). The rest are **not sources** — they are magazine articles *about* collections, or essays. That distinction matters: an Open Culture post is a pointer, and the thing worth recording is the archive it points at, since that archive is what an adapter would talk to.

| Lead | What it actually points at | Verdict |
| ---- | -------------------------- | ------- |
| [Margolies roadside architecture](https://www.openculture.com/2026/07/free-photos-from-john-margolies-archive-of-americana-architecture.html) + [`loc.gov/pictures/?q=mrg`](https://www.loc.gov/pictures/search/?q=mrg&st=gallery) | **Library of Congress**, 11,708 images, "free to use and reuse". Same collection, two links. | ✅ Folded into the **LoC** row — this is its concrete first trial. |
| [Carl Jung's visionary art](https://www.openculture.com/2026/07/visionary-mystical-art-of-carl-jung.html) · [Jung on tarot](https://www.openculture.com/2023/10/carl-jung-on-the-power-of-tarot-cards.html) | *The Red Book / Liber Novus*. **Not public domain** — Jung died 1961 (so life+70 runs to ~2031), the facsimile was published by Norton in 2009, and the manuscript is estate-controlled. No institution offers it under an open license. | ❌ **Licensing trap.** Ambit's imagery bar is PD/CC0/open, and this clears none of it. Historic *tarot decks* are a separate matter — Waite–Smith (1909) is PD in the US and reachable via existing sources. |
| [Japanese woodblock prints of the body](https://www.openculture.com/2026/07/japanese-woodblock-prints-illustrate-the-human-body.html) · [1,300 wildlife illustrations](https://www.openculture.com/2026/07/explore-1300-beautiful-wildlife-illustrations-from-the-19th-century.html) | Both are 19th-c. institutional holdings of exactly the kind **Wellcome** (medical/anatomical) and **BHL** (natural history) already cover — Wellcome is a committed source, BHL an existing candidate. | 🟡 **No new adapter needed.** Treat as *seed-query* material: if this texture is missing from the feed, the fix is a better `seedQueries` cell on the sources we have, not a new source. Worth checking against the corpus before assuming it's absent. |
| [Long Now — Earthquake Lessons](https://sb.longnow.org/SB_homepage/Earthquake_Lessons.html) · [strategy+business 06109](https://www.strategy-business.com/article/06109) | Individual Stewart Brand essays on named sites. No API, no open license, single-author copyrighted work. | ❌ **Not a source.** Two essays is not a corpus, and neither site offers a licensed feed. If these are here as *taste* references — the register Ambit's text items should hit — that is a genuinely useful signal, but it belongs in the feel-tuning notes, not the adapter backlog. Worth confirming which was meant. |
| [Getty photographs](https://www.getty.edu/museum/photographs/) · [Getty Primo portal](https://primo.getty.edu/primo-explore/search?vid=GETTY_PORTAL) | Browse UIs over the same holdings as the Getty API row. Primo is the *library* catalogue (a discovery layer, not an open-image endpoint). | 🟡 Covered by the **Getty** row. The API, not the portal, is the integration surface. |

## Designated blogs (08-20-26)

> **These do not go through the trial loop above.** Blogs are not APIs: they are handled by the
> planned **blog-adapter family** (`SPEC.md` §6.1, `docs/BUILD_PLAN.md` step 6.3) — a shared scraper
> core plus per-blog config — and they are displayed as **link cards**: image or short excerpt,
> Ambit's own description, a blurb about the article, a `from: <blog>` credit, and a prominent link
> to the original. Ambit hosts no reformatted articles and makes no fair-use claim.
>
> **Their bar is different too:** not "does it have a filterable open-license API" but **scrape
> feasibility + the link-card posture** — is the content reachable politely, and is
> credit-plus-link-out an honest way to show it? **Scrape etiquette is a real gate, and there is
> already a worked precedent: artvee was cut** on 08-20-26 (see the Candidates table) because its
> `robots.txt` runs an AI block list. A site that machine-readably refuses agents does not become a
> designated blog just because the works it hosts are public domain.
>
> **The etiquette policy, as built (6.3, `docs/PHASE6_DESIGN_6.3.md` §8; enforced by `config/blogs.ts` and `services/sources/robots.ts`):**
> 1. `robots.txt` is checked before a blog is designated and **re-checked on every ingest run**; `Disallow: /` for `*` or for our agent aborts the walk and reports it.
> 2. Identify honestly — the shared `USER_AGENT`, which names Ambit and a contact.
> 3. Rate: ≥500 ms between requests, sequential per host.
> 4. Cadence: the ordinary ingest cron; no separate crawl schedule.
> 5. Removal on request: `bun run ingest --source <blog> --prune` for posts the blog has removed; a named item is a manual delete in FK order. No suspended-items mechanism — YAGNI for an invite-only app.

| Blog | Type | Posture | Status | Notes |
| ---- | ---- | ------- | ------ | ----- |
| **doorofperception.com** | image — art / visionary culture | Link card: credit + link out | **✅ Live (6.3, 08-27-26)** — WP REST corpus-walk adapter, 318 link cards, `body` null; the archive scrape below is retired by D2. *Earlier notes:* | The first corpus, and the reason the strategy exists. **11,572 images already on disk**, in `ambit-archive` under `storage/sources/doorofperception/`, with `index.csv` (11,584 rows: `file, post_slug, post_url, original_url, mime, downloaded`) as the **attribution source** — do not delete or regenerate it. Currently ingested as *archive* items; migrating them here retires 85% of the archive corpus. Sequenced after archive A.5/A.6 and after 6.3's design session, and it **owns the Ambit-side dedupe question**, since those items may already be in the corpus via the archive adapter. |
| **50watts.com** | image — book art, illustration, obscure design | Link card | **❌ Cut 08-25-26** — `User-agent: * / Disallow: /`, and its WP REST API 403s regardless of UA. The artvee rule. | Long-running curated blog with dense per-post image sets and real editorial writing — close to the exact texture the README's "old Tumblr art blogs" reference names. Scrape feasibility unchecked. |
| **thisiscolossal.com** | image — contemporary art & visual culture | Link card | ✅ **Keep — Ben's verdict 09-01-26; on `main`, full walk written 09-02-26** | **Full walk, 09-02-26 (local):** 8,818 offered · 83 floored (thin-summary) · 8,732 curated · **6,075 written @ 8.70 avg, 97.5% ≥ 8** · 2,657 refused (30%) · all 16 topics (architecture 933, zoology 826, portraiture 720). 3 curator errors: two heroes over OpenRouter's 30 MB limit (animated GIFs), one 502. ~3 GB of 2000 px originals per full curation, 38 min. *Sample (09-01-26, `--quota 150`, `stats:walk`):* 139 curated @ 8.65, 97% ≥ 8, 116 classified into 13 topics, 23 refused @ 8.48 — the strongest sample of any source. Built as `thisiscolossal.ts` = `wp-rest` factory + `blogs.ts` row; fixture of three real posts. *Probe notes:* High post volume, consistently strong imagery, and the most *current* of the set where the rest skew historical. `robots.txt` is `Allow: /` for `*` (not a hard block like 50watts/artvee) — it does carry Cloudflare's templated named-bot disallow list, which includes `ClaudeBot` specifically, though Ambit's adapters identify as `Ambit/0.1 (...)`, not `ClaudeBot`, so the shipped `robotsDisallowsAll()` check passes clean; worth surfacing to Ben as a judgment call rather than treating it as mechanically resolved. WP REST API is live at the canonical `www.thisiscolossal.com` host (bare domain 301s there) — 8,817 posts, `_embed=wp:featuredmedia` resolves to ungated full-2000px originals. Byte-for-byte the same shape doorofperception's adapter already handles — near drop-in reuse of the shared scraper core. |
| **lastmuseum.com** | image — art | Link card | ❌ Cut 09-01-26 | Live (Cloudflare-fronted Next.js app), but it's not a blog — it's a semantic search UI over "5.8 million artworks from the world's great museums," no posts/author voice/article unit to excerpt into a link card. Same exclusion category the doc already applies to Open Culture ("an aggregator, not a primary source"). If the index behind it is itself interesting, that points at whatever museum APIs back it, not at an adapter of its own — unidentified in this probe. |
| **thingsorganizedneatly.tumblr.com** | image — knolling / arranged objects | Link card | ✅ **Keep — Ben's verdict 09-01-26; merged, full walk written** | **Full walk, 09-01-26 (local):** 5,267 offered · 3,547 floored (3,320 thin-summary; dup-title 0 after the walk-source exemption) · 1,720 curated · **891 written @ 7.90 avg, 71.8% ≥ 8** · 829 refused. Below the 1,300–1,500 projection: the archive thins with depth. Derived titles keep `ed:` / `SUBMISSION:` prefixes — cosmetic, unfixed. *Sample:* **Adapter + trial sample, 09-01-26** (`things-organized-neatly.ts`, built to `docs/HANDOFF_tumblr-walk.md`; postscript there has what differed from the plan). Dry-run of the newest 150 offered: 1 `toItem` error (an `answer` post, by design) · **floored 67** (52 thin-summary, 9 dup-title, 6 bare-title) · **83 curated @ 8.02 avg, 4–9, 78% ≥ 8** · **55 classified (avg 8.18) into 14 of 16 topics** — zoology 10, machines 9, typography 8, architecture 6 lead; poetry and geology 0 — · 28 honestly refused (avg 7.71: Prada ads, a Hoppy Easter still life, flat lays — good images with no home among the sixteen). Would insert 55 of 150 (37%); doorofperception's ratio was 82% because WordPress excerpts are written, not captioned. **Two things for the verdict.** (1) The archive gets thinner with depth — a 200-post sample across offsets 0/1000/3000/5400 had 60% of captions under the floor and 26% empty, against 35% under on these newest 150 — so a full walk of 5,522 should land somewhere near 1,300–1,500 items, not 2,000. (2) **dup-title floors a blog's series posts**: "Andy Goldsworthy" ×3 and "A Look Inside the Peeps Factory" ×3 all went, because a caption-derived title collides where a WP title never did — 9 of 150, and Goldsworthy is exactly the texture wanted. Accept, or exempt walk sources from that one rule: Ben's call, not made here. *Earlier notes:* Tumblr, so posts are short: image plus little or no article text. **Re-probed 09-01-26** (Ben asked for this one by name): `/api/read/json?num=N` still 200s, 5,522 posts, two post types in practice — `photo` (structured `photo-url-1280`/`photo-caption`) and `regular` (image embedded in `regular-body` HTML, needs a first-`<img>` extraction). A 20-post sample put roughly half under the 60-char thin-summary floor — real yield, no new mechanism needed; **doorofperception's own precedent ("never padded here") already answers the "nothing to blurb" edge case** — thin captions floor like any museum stub, no LLM-synthesis machinery required. One thing surfaced worth a deliberate yes/no rather than a silent pass: `robots.txt` here names `ClaudeBot` and `anthropic-ai` explicitly with `Disallow: /` — confirmed as **Tumblr-platform boilerplate, not this blog's own policy** (the identical list appeared verbatim on thisisnthappiness.com, a different Tumblr blog on a different domain); Ambit's `Ambit/0.1 (...)` UA isn't named, so it's the same mechanical pass as thisiscolossal's Cloudflare list, just a closer call since it names Anthropic specifically. |
| **thisisnthappiness.com** | image — street art / murals, curated by "nevver" | Link card | 🔵 Untried — *probed 09-01-26* | Also a **Tumblr blog** (custom domain, not `*.tumblr.com` — same legacy `/api/read/json` API, confirmed live), 108,968 posts, one of Tumblr's longest-running curated art blogs. Same platform-wide named-AI-bot robots.txt as thingsorganizedneatly (see that row). Genuinely image-first, often literally caption-less (multi-image NPF blocks with zero prose) — a harder version of the same "nothing to blurb" question, worth trialing only after thingsorganizedneatly's Tumblr-walk adapter exists to extend. |
| **streetartnews.net** | image — street art | Link card | ⏸️ **Parked — Ben's verdict 09-02-26, on sequencing not quality** (adapter on `main`, in `SUSPENDED_SOURCES`; no rows written). Revisit **after Cut 2**: the sample is level with thingsorganizedneatly (7.81 avg, 71% ≥ 8, 0 floored), but a full walk would take ~43% of `mythology` and put the two contemporary-art blogs at ~75% of it, and its ~4,120 un-homed rows would dominate the tag histogram Cut 2's promotion reads — letting one blog set the vocabulary. If Cut 2 creates a `street art` topic this stops being a capture risk and becomes the way to fill it | **Adapter + trial sample, 09-02-26** (`streetartnews.ts` = the `wp-rest` factory over a `blogs.ts` row; fixture of three real posts). Newest 150 offered, dry-run: **0 `toItem` errors · floored 0** (nothing thin — excerpts run 355–382 chars, the fattest of any blog yet) · **150 curated @ 7.81 avg, 4–9, 71% ≥ 8**, histogram `4:1 6:3 7:39 8:86 9:21` · **87 classified (avg 7.95) into 10 of 16 topics** — mythology 22, architecture 19, typography 13, botany 9, portraiture 7 lead; astronomy / cartography / ceramics / geology / textiles / ancient-history all 0 · **63 refused (avg 7.62)** · would insert 58%. Robots re-verified 09-02-26, unchanged and still the cleanest probed. Canonical host confirmed **bare** (`www` 301s to it), 9,509 posts. **Three things for the verdict.** (1) **The refusals are not the tail.** At 7.62 avg they are barely below the classified 7.95, and they include two 9s (Snik's "Still Life", Ai Weiwei's "Don Quixote") — this is a genre with no home among the sixteen topics, not a soft corpus. 42% of a strong blog is dropped for topic fit alone. (2) **`mythology` is the single largest bucket (22 of 87) for a street-art blog** — some genuinely earn it ("Ettore and Andromaca Bring Ancient Myth to the Streets"), but it reads like the classifier's nearest available shelf for figurative murals. Worth an eyeball of those 22 before a full walk. (3) **The predicted skateboarding drift showed up and the classifier handled it** — "Argentina, Thailand and Russia Take Street Skateboarding Podium at BRICS Skate Cup 2026" scored 7 and was refused, no topic gate needed. Separately, the blog carries some `Artist Interview:` posts (the juxtapoz/hyperallergic content-fit concern in miniature); both in this sample were refused. **Cut 1 re-read, 09-02-26** (branch rebased onto `main` after the vocabulary-growth merge; `--dry-run --quota 150` then `stats:walk`, both free from the curation cache). The numbers barely moved — **150 curated @ 7.81 avg, 4–9, 71% ≥ 8**, `4:1 6:3 7:40 8:85 9:21`, **85 classified (avg 7.95) / 65 un-homed (avg 7.62)**, 10 of 16 topics, 0 floored — but the *meaning* did: under Cut 1 the blog stores **100% of what it offers**, and the 43% is a fact about the vocabulary, not a loss. What the re-read adds that the first sample could not: **the un-homed tag histogram** — `street art 24 · bold color 7 · dubai 7 · graphic 7 · uae 7 · united arab emirates 7 · urban art 7 · elleresqphoto 6 · elleresqphotowall 6 · vibrant palette 6 · spain 5 · whimsical 5`. **Three readings of it.** (1) **`street art 24` + `urban art 7` is independently corroborated** — thisiscolossal's own Cut 1 re-walk the same day produced `street art 184` from a completely different corpus, so the cluster is real and cross-source, not an artifact of this blog. (2) **Two new classes of tag noise for Cut 2's promotion to filter**, on top of Tumblr's `submission`: photographer handles (`elleresqphoto`, `elleresqphotowall`) and **geography — and `dubai` / `uae` / `united arab emirates` are three synonyms for one place, which would triple-count in any naive frequency ranking.** (3) The classifier is *not* force-fitting: the predicted skateboarding drift (`BRICS Skate Cup`) landed un-homed, as did both `Artist Interview:` posts. **One defect the sample exposes:** `Artist Interview: Dan The Automator Nakamura` appears **twice at score 6** and both copies survive, because walk sources are exempt from the dup-title floor (the exemption added for thingsorganizedneatly's caption-derived titles). A real duplicate now reaches the corpus — worth a narrower rule before a 9,509-post walk. **Scale note for the verdict:** at 9,509 posts and 0% floored this is the largest walk source yet proposed — a full walk would exceed thisiscolossal's 8,737 rows. *Earlier notes:* Cleanest robots.txt of any blog probed to date — no named-bot section at all, just a `/wp-admin/` disallow. WP REST live and ungated, full-res images, 9,505 posts. Near drop-in for the shared WP-REST scraper core. One drift risk: the top post at probe time was skateboarding-competition news, not street art — worth a seed-query/topic gate to keep the corpus on-brand. |
| **spoon-tamago.com** | image — Japanese art & design | Link card | 🔵 Untried — *probed 09-01-26* | Cleanest content-fit of the new batch: posts run ~170–210 words, already blurb-sized, no excerpt-trimming judgment call needed. WP REST live and ungated (behind a Sucuri WAF that passed clean at a polite pace — worth keeping the ≥500ms/sequential rule strict here specifically), 4,075 posts, actively maintained. Source images are modest resolution (some `full` sizes as small as 640×480) — fine for a tile, not for a large hero crop. |
| **mossandfog.com** | image — design, nature, visual inspiration | Link card | ⏸️ **Parked — Ben's verdict 09-01-26** (adapter on `main`, in `SUSPENDED_SOURCES`; no rows written) | **Adapter + trial sample, 09-01-26** (`mossandfog.ts` = the `wp-rest` factory over a `blogs.ts` row; fixture of three real posts). Newest 150 offered, dry-run: 0 `toItem` errors · floored 14 (all thin-summary) · **136 curated @ 6.60 avg, 1–9, 41% ≥ 8**, with a cluster of 36 at score 4 · **77 classified (avg 7.30) into 10 of 16 topics** (architecture 31, machines 20) · 59 refused (avg 5.68) · would insert 51%. The tail is advertorial — "The Top Place to Buy Tyres in Edinburgh" (1), artificial turf (2), a business post published twice (2) — which the curator refuses, but the middle is soft. Sound adapter, soft corpus; un-parking is one line in `suspended-sources.ts`. WordPress.com-hosted: **27,567 tags**, which is why `wp-rest.ts` resolves tag names lazily per page rather than up front like doorofperception. *Earlier notes:* Same shape as thisiscolossal/mossandfog's own WordPress.com-hosted infra: WP REST live and ungated, 7,537 posts, daily cadence, one-paragraph blurb per post. Clean robots.txt, no gotchas found. Near drop-in. |
| **juxtapoz.com** | both — contemporary art magazine | Link card, *but* | 🟡 Parked 09-01-26 | Not WordPress — Joomla + K2, so the only walk mechanism is a thin, undocumented RSS feed (no `_embed`-equivalent, no total-count, pagination unconfirmed) — a real integration-effort step down from every WP-REST blog in this table. Worse, the content itself splits: `/news/painting|graffiti|street-art/` posts are exhibition-announcement press-release copy (link-card-honest), but `/news/magazine/` posts are **real Q&A interviews** (~1,500+ words of authored conversation) that a link card would strip down to an image-aggregator card. Needs a URL-segment filter and a real feed mechanism before it's trial-worthy, not a drop-in. |
| **hyperallergic.com** | both — arts criticism & journalism | Link card, *but* | ❌ Cut 09-01-26 | The best-behaved site probed yet on access grounds — Ghost-powered, clean robots.txt, and its own `/llms.txt` explicitly welcomes AI/LLM tooling (`.md` suffix serves clean markdown). **Cut anyway, on content-fit, not access**: the sampled content across its RSS feed is uniformly reported journalism (on-the-ground scenes, named sources, quotes, ~1,000+ words) — real arts criticism, not an image round-up. A link card (photo + blurb + credit) would misrepresent original reporting as a visual-feed item exactly the way the link-card posture is designed to avoid. Access being effortless makes this a harder call to walk away from, not an easier one — but the posture doesn't fit regardless of how welcoming the site is. |
| **openculture.com** | both — aggregator | Link card, *but* | Parked | **An aggregator, not a primary source.** Its posts are pointers at some institution's archive — exactly the distinction the "Leads, not sources" table above draws — and crediting Open Culture for an image the Library of Congress holds is the wrong credit. May belong in the seeding list below rather than as a designated blog; decide at 6.3. |
| **Public Domain Review** | both — essays + PD imagery | **Image items + articles + link cards** (see notes) | ✅ **KEPT — Ben's verdict 09-02-26** (all four phases; full walk the same night, **1,624 rows**; local only until the next deploy) | **Adapter + three trial samples, 09-02-26** (`pdr.ts` — NOT a blog: its images are public domain and its own text is CC BY-SA 4.0, so it gets its own `config/pdr.ts` rather than a `blogs.ts` row). **Shape:** a corpus walk over Gatsby's `page-data` JSON — four one-shot indexes (1,255 collections; 343 essays; Conjectures 21; Curator's Choice 29 — the series are *not* in the essays index), cursor `<phase>:<offset>`, then per-record hydration cache-aside into `.cache/pdr/<kind>/<slug>.json` because every detail response is 0.5–1.2 MB of which only 8–30 KB is the record (a full uncached walk is ~1.7 GB from a non-profit's CDN; cached, a nightly walk is four index fetches). **Rights policy in `walk()`:** a collection whose digital copy any source marks `Non-commercial` is dropped before it becomes a raw — 5 of the newest 150. **Two projections:** a collection is an `image` item that also carries its Preamble as `body` (rendered under the picture, introduced by `ReuseNotice`); an essay is an `article` when `Publication_Rights === "CC-BY-SA"` and a **link card** otherwise. The PDR **Blog** (76 org-news posts, no images) is deliberately out, as are footnotes, bibliographies and inline image blocks.

**Sample 1 — `stats:walk pdr --quota 150` (newest collections):**<br>`toItem errors 0 · floored 1 (bare-title 1) · curated 149`<br>`curated: avg 8.73 · min 7 · max 10 · ≥8 97%`<br>`histogram: 7:5  8:33  9:108  10:3`<br>`classified 135 (avg 8.76) · un-homed 14 (avg 8.50) · stored 99% of offered · un-homed 9% of stored`<br>`topics (15/16): mythology 64, zoology 26, ancient-history 21, typography 17, botany 15, portraiture 14, machines 10, cartography 6, poetry 5, textiles 5, astronomy 5, music 4, the-ocean 4, architecture 3, geology 2`<br>`un-homed tags: 20th century 9 · 19th century 6 · books 6 · customs & culture 6 · images 6 · engraving 3 · music & arts 3 · photography 3 · 16th century 2 · 17th century 2`

**Sample 2 — `stats:walk pdr --cursor e:0 --quota 60` (newest essays):**<br>`toItem errors 0 · floored 0 · curated 60`<br>`curated: avg 8.32 · min 7 · max 9 · ≥8 95%`<br>`histogram: 7:3  8:35  9:22`<br>`classified 51 (avg 8.33) · un-homed 9 (avg 8.22) · stored 100% of offered · un-homed 15% of stored`<br>`topics (12/16): mythology 18, ancient-history 15, architecture 7, poetry 6, zoology 5, machines 5, portraiture 5, botany 3, typography 3, music 1, textiles 1, cartography 1`<br>`un-homed tags: culture & history 6 · art & illustration 3 · books 3 · film 2 · france 2 · literature 2 · photography 2 · 19th-century art 1 · alcohol 1 · amanuenses 1`

**Sample 3 — `stats:walk pdr --cursor x:0 --quota 50` (all 21 Conjectures, then 29 Curator's Choice — the phase rolls over mid-sample):**<br>`toItem errors 0 · floored 0 · curated 50`<br>`curated: avg 8.02 · min 7 · max 9 · ≥8 72%`<br>`histogram: 7:14  8:21  9:15`<br>`classified 40 (avg 8.18) · un-homed 10 (avg 7.40) · stored 100% of offered · un-homed 20% of stored`<br>`topics (12/16): mythology 12, ancient-history 8, portraiture 6, machines 6, architecture 3, typography 3, poetry 3, botany 3, zoology 2, music 2, geology 1, astronomy 1`<br>`un-homed tags: conjectures 8 · curator’s choice 2 · painting 2 · 18th century 1 · academic 1 · anatomical illustration 1 · archive 1 · art 1 · art concret 1 · art history 1`

**What the un-homed items are about (the post-Cut-1 eyeball question).** They are not junk — they score as high as the classified ones (8.50 / 8.22 / 7.40 against 8.76 / 8.33 / 8.18) and include *Pyramid of the Capitalist System* (1911), Soutine's still lifes, Willem ten Rhijne's acupuncture diagrams and Albert Kahn's autochromes. **The two halves of the corpus bring different vocabulary:** the collections' un-homed pile is dominated by **medium and epoch** terms (`20th century`, `19th century`, `images`, `engraving`, `photography`), while the essays' is dominated by **subject** terms (`culture & history`, `art & illustration`, `books`, `literature`, `film`). Ambit's sixteen topics are a *subject* axis, so only the second half is promotion material on the current axis — see the axis question in the verdict report.

**Link-card share — the one number the plan got wrong, and it matters.** The plan's 32-essay random sample predicted ~10% link cards; across the 110 essays actually hydrated it is **42%**, and it is entirely a composition effect:<br> • **essays index (newest 60): 27% cards** — 16 `Custom License`, 0 unlabelled. Recent PDR essays are far more often adapted book excerpts than the older random sample suggested.<br> • **Conjectures (all 21): 5% cards** — 20 of 21 CC BY-SA.<br> • **Curator's Choice (all 29): 100% cards** — *every one is unlabelled*. These are guest posts by institutional curators and PDR licenses none of them, so the whole series lands as link cards.<br>Two `Custom License` essays (`roma-lister-aradia`, `the-blinkered-flaneur`) carry a `License_Note` that says the text *is* CC BY-SA. `essayIsOpen` reads only the explicit label, so both become cards — conservative and never over-claiming, but a deliberate under-claim worth knowing about.

**Also proven live:** the one non-ASCII slug (`d-a-rovinskiis-collection-of-russian-lubki-18th–19th-century`, an en-dash) encodes to `%E2%80%93` and fetches; the rights policy drops `fixed-stars` (Bibliothèque nationale de France) with `0 raw` while the cursor still advances; both no-image collections (`hands-1944` at offset 939, `a-midsummer-schottische` at 915 — 2 of 1,255) throw a named `toItem` error rather than storing a pictureless card; the cache turns a 3.5 s five-record probe into 0.6 s; the phase roll-over (`x:19` → `k:0`) and the end of the walk (`k:27` → `(end)`) both observed.

**FULL WALK 09-02-26 (local), after Ben's Keep:** 1,648 records across the four indexes → **19 excluded on rights** → 2 `toItem` errors (`hands-1944`, `a-midsummer-schottische` — the only two collections without a featured image) → 1,627 offered → 3 floored (1 bare-title, 2 thin-summary) → **1,624 rows stored @ 8.39 avg, 87% ≥ 8** (histogram `3:1 4:5 6:3 7:202 8:563 9:827 10:23`). By shape: **collections 1,231 image items @ 8.50 · essays-as-articles 318 @ 7.93 · essays-as-cards 75 @ 8.63**; 1,547 rows carry a body. All sixteen topics filled (mythology 589, ancient-history 232, portraiture 217, typography 171 … ceramics 16). **186 rows (11%) un-homed**, zero memberships, invisible to the feed until Cut 2 — verified. `item_topic.origin` is `curator` on all 2,208 memberships, never `seed` (the plan's §1a hazard, checked before the walk).

**Correction to the sampled link-card share above.** The 42% figure was a sampling artefact: the 110 essays cached at that point were the newest 60 plus *all* of both series, so Curator's Choice — the 100%-card phase — was 26% of the sample but is only 7% of the 393-essay corpus. **The true corpus-wide share is 75 of 393 = 19%.**

**One bug found by the full walk, fixed the same night.** Exactly one slug in 1,648 carries a trailing slash (`get-thee-to-a-phalanstery-…-make-lemonade/`) and it is the *record's own* `Slug` field, so it hit the cache path, the detail URL (`encodeURIComponent` → a 404-ing `%2F`) and the stored `sourceId` at once. The first full walk died on it at cursor `e:100`, before Conjectures or Curator's Choice were walked at all. `slugOf()` now normalises at the wire boundary; `cachePath()`'s guard was deliberately left absolute rather than taught to accept a slash. |

### Single posts worth seeding

Individual articles, not whole blogs — the kind of thing that seeds one item or one topic rather than justifying an adapter. Kept here so they don't get lost in the raw dump.

- [Open Culture — 1.8 million free works of art from world-class museums (meta-list)](https://www.openculture.com/2016/05/1-8-million-free-works-of-art-from-world-class-museums-a-meta-list.html) — a list *of* archives; mine it for candidate APIs rather than ingesting it.
- The 08-20 batch's other Open Culture posts (Margolies, Jung, Japanese woodblock, wildlife illustrations) are triaged in the Leads table above — most point at institutions Ambit already reaches.


## Untriaged raw notes

_Older dump, kept as-is — not yet run through the bar above._

#
Metropolitan Museum of Art
Art institute of chicago
Art Search
some sort of place holder, should be local function maybe?
extinct animals api
free movie api
free uv index api
hacker news api
nga.goc national gallery
getty images
amazing endemic species
holidays api
wikipeida events on this day
old ass news papers - https://www.loc.gov/collections/chronicling-america/about-this-collection/technical-information/?__cf_chl_f_tk=FVL9obx3BUMLHxwLD0AD8XK9_vBQGDmSQkW3HtTvaPE-1783187592-1.0.1.1-Hf3AvZHnS1TsqKMlr1u9siaDH_ixpwIp_T0HdFaXfug
checkout out archive.org, maybe scrape some public info and stand up my own api?
